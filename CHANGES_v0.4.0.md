# v0.4.0 — троттлинг обратки: мост стал фильтром, а не трубой

## Проблема

Обратка сервер→меш гнала в LoRa **всё**, что прислал TAK-сервер. Существующие фильтры
против потока не помогали:

- дедуп по `uid|time` схлопывает только один и тот же broadcast, пришедший на N
  cert-сокетов. Движущийся игрок шлёт PLI с новым `time` → каждый раз новый ключ → мимо;
- эхо-фильтр по callsign режет только **наших** мешевых, вернувшихся с сервера.

Физика (LONG_FAST, дефолт прошивки; пакет ~60 Б = **~680 мс эфира**):

| Игроков на сервере (PLI раз в 5 с) | Надо | Потолок сети | Перегруз |
|---|---|---|---|
| 5 | 60 пкт/мин | ~88 пкт/мин при 100% эфира | 7× |
| 20 | 240 пкт/мин | | **27×** |
| 50 | 600 пкт/мин | | **68×** |

Эфир общий на всю сеть, и каждый пакет соседи ещё и ретранслируют. Итог перегруза —
не «взрыв», а деградация: очередь рубится случайно (в т.ч. важное), задержки в минуты,
маяки выборов (раз в 30 с) теряются → шлюз флапает между активным и standby.

## Решение

**LoRa не может быть зеркалом TAK-сервера.** Меш тянет «медленную обстановку»: где мои,
обновление раз в минуту-две. Мост теперь это осознаёт.

### Бюджет эфира — автоматически из пресета ноды

`LoraParams` считает airtime по формуле из даташита SX127x и берёт **8% эфира**
(`AIR_SHARE`) — ниже EU-duty 10% и сильно ниже порога занятости канала ~25%, остальное
оставляем мешу на его трафик и ретрансляции.

Пресет читается с живой ноды: `IMeshService.getChannelSet()` → `ChannelSet.lora_config`
→ `modem_preset` (кастомные SF/BW/CR тоже). Бюджет едет за пресетом сам:

| Пресет | Потолок | Бюджет обратки |
|---|---|---|
| LONG_FAST (дефолт) | ~88 пкт/мин | **7** |
| MEDIUM_FAST | ~298 | **23** |
| SHORT_FAST | ~991 | **79** |

Пресет не прочитался → фолбэк на LONG_FAST (самый узкий из ходовых), т.е. ошибаемся
в безопасную сторону.

### `ObratkaThrottle`

- **Приоритет чата** — чат/тревога вперёд позиций и мимо порога движения.
- **Порог движения** (настраиваемый, дефолт 50 м) — стоящий контакт трафика не ест.
  Раз в 5 мин позиция уходит всё равно, иначе контакт протухнет и пропадёт с карты ATAK.
- **Коалесcинг** — на контакт только ПОСЛЕДНЯЯ позиция, не очередь. В меш уедет актуальная
  точка, а не хвост устаревших.
- **Token bucket** по бюджету — жёсткий потолок.
- **Честный round-robin** по времени ожидания — болтливый не заморит остальных.

### Счётчики

Бюджет, пресет, «ждёт эфира / подавлено (стоит) / схлопнуто».

## Чего сознательно НЕ делаем

**Геофильтр и whitelist.** На сервере все рядом и нужны все контакты. Следствие: бюджет
делится между всеми, и частота обновления каждого получается сама собой —
`бюджет / число контактов`. На LONG_FAST с 20 контактами это ~раз в 3 минуты на человека.
Это честный физический потолок LoRa: хочешь чаще — MEDIUM_FAST (тогда ~раз в 50 с, но
дальность падает) или меньше контактов.

## Проверено

- 40 юнит-тестов, включая «под завалом в 100 контактов темп режется до бюджета»,
  коалесcинг, приоритет чата, честность, refresh неподвижного.
- Вживую (Кола, OnePlus, Android 16, нода на LONG_FAST):
  `Пресет ноды: LONG_FAST → бюджет обратки 7 пкт/мин` — прочитан с реальной ноды;
  позиция с URPC доехала сквозь троттл в меш (`bridge → меш` тикнул).

## Известное ограничение

Троттл висит только на пути **RX (cert)** — рекомендованном и дефолтном. Режим
`Multicast` (запасной, на практике мёртвый) идёт мимо троттла.
